<analysis>**original_problem_statement**: The user originally requested overall app performance optimization and resilience. In this session, the user reported a bug where bots leave multiple comments on a single feud, which looks unrealistic. They requested limiting bots to one contribution (either a top-level comment or a reply) per feud. 

PRODUCT REQUIREMENTS:
- Ensure a bot leaves a maximum of 1 contribution per feud.
- Clean up existing duplicate bot comments/replies in the database.
- Ensure the duplicate prevention logic holds up under concurrent/burst bot operations.

**User's preferred language**: **Italian** (You MUST automatically detect the user's primary language from their messages inside the <trajectory> tag and INSTRUCT next agent to respond in the user's preferred language only. Proceed in **Italian**).

**what currently exists?** 
A highly optimized full-stack Expo frontend and FastAPI backend social application (Populus). The app utilizes an AI bot engine () to generate activity on feuds. Application-level logic was added to restrict bots to one comment/reply per feud, and a startup cleanup script was executed to remove existing duplicates. However, a race condition exists during bot burst (concurrent) operations, allowing duplicates to still bypass the application-level check.

**Last working item**:
    - Last item agent was working: Fixing the bot duplicate comment bug. The agent added application-level checks in  and  and cleaned the DB. However, the  reported that a race condition exists under burst usage, meaning the invariant does not hold concurrently. 
    - Status: IN PROGRESS
    - Agent Testing Done: Y
    - Which testing method agent to use?: backend testing agent (You MUST call the  to re-run the backend test suite after fixing the race condition).
    - User Testing Done: N

**All Pending/In progress Issue list**:  
  Issue 1: Bot Multiple Comments Race Condition (P0)
     - Attempted fixes: Added application-level  checks in Python. Failed because parallel async tasks during a burst bypass the check before the DB write completes.
     - Next debug checklist: Check  startup indexes and . You need to implement a concurrency fix, such as adding a compound unique index in MongoDB for  for bot comments/replies, or implementing an async lock in the bot engine during burst actions.
     - Why fix this issue and what will be achieved with the fix? Enforces the core requirement that bots cannot spam a single feud under heavy concurrent load.
     - Status: IN PROGRESS
     - Is recurring issue? Y (Failed  validation)
     - Should Test frontend/backend/both after fix? Backend (via )
     - Blocked on other issue: None

  Issue 2: Ghost/Strange Bot Names Visible in Frontend (P1)
     - Attempted fixes: Verified DB is clean in the previous fork. Issue is local cache ( / Apollo) on the user's device.
     - Next debug checklist: Await user confirmation if reloading/clearing cache resolved the issue.
     - Why fix this issue and what will be achieved with the fix? Ensures the user sees the accurate clean state.
     - Status: USER VERIFICATION PENDING
     - Is recurring issue? N
     - Should Test frontend/backend/both after fix? Frontend
     - Blocked on other issue: None

**In progress Task List**: 
  *(None active)*

**Upcoming and Future Tasks**
  Task 1: Complete Frontend Refactoring (Phase 3 of Cleanup) (Priority P2)
     - Where to resume: , , 
     - What will be achieved with this? Splitting massive files into smaller, reusable components. 
     - *Note: Paused previously due to high risk of breaking deep linking. Proceed ONLY if explicitly requested by the user.*

**Completed work in this session**
- Added application-level verification to prevent duplicate bot comments in  (DONE).
- Created and executed a one-shot DB cleanup function in  to remove legacy duplicate comments (DONE).

**Earlier issues found/mentioned but not fixed**
   *(None)*

**Known issue recurrence from previous fork**
  - Issue recurrence in previous fork: Deep link scrolling & mention logic inside .
  - Recurrence count: 3+
  - Status: RESOLVED (Do not touch unless explicitly requested).

**Code Architecture**
- : Core logic for bot personas, AI text generation, and DB insertion for comments/replies.
- : FastAPI entry point containing DB index creation, cleanup scripts, and core route registrations.

**Key Technical Concepts**
- FastAPI async architecture interacting with MongoDB.
- Race conditions: Async loops iterating over  and  concurrently can trigger multiple DB writes before the first read acknowledges the comment exists.

**key DB schema**
  -  / : Standard collections where bot interactions are stored. To strictly enforce 1 bot per feud, a unique constraint mechanism at the DB level is highly recommended over purely application-level logic.

**changes in tech stack**
  - None.

**All files of reference** 
  -  (Updated to include duplicate checking logic)
  -  (Updated to include a startup duplicate cleanup script)
  -  (Created by testing_agent to validate the fix)

**Areas that need refactoring**: 
  -  and  are massive and candidates for Phase 3 refactoring.

**key api endpoints**
  - : Triggers mass concurrent bot actions.

**Critical Info for New Agent**
- **Language Requirement:** MUST communicate with the user in **Italian**.
- **OpenAI Quota:** During the session, the backend logs recorded . LLM bot generations might fail right now. Focus strictly on fixing the concurrency/database constraints so the tests pass, even if the LLM mocks the output.
- **Testing Agent Feedback:** The  explicitly returned  because the race must be fixed for the invariant to actually hold under normal burst usage. Your first action must be addressing this DB index/lock issue.

**documents and test reports created in this job** 
  - 
  - 
  -  (Updated/Verified)

**Last 10 User Messages and any pending HUMAN messages** 
 - (User) Please use ask_human tool and confirm your plan now. -> (Agent) Shared plan via tool. *(Completed)*
 - (User) un bot non deve lasciare tanti commenti sotto una stessa faida come accade ora... (A bot shouldn't leave many comments under the same feud like it does now...) -> Agent applied code logic and cleanup scripts, but tests failed due to race conditions. *(IN PROGRESS - P0)*

**Project Health Check:**
- Broken: Concurrent bot bursts bypass duplicate comment limitations due to a race condition.
- Mocked: None.

**3rd Party Integrations** 
	•	Emergent-managed Google Auth
	•	OpenAI GPT Models — Uses Emergent LLM Key (Currently hitting rate limits)
	•	Firebase (Android Push) — requires User 
	•	Google Mobile Ads — requires AdMob keys

**Testing status**
  - Testing agent used after significant changes: YES
  - Troubleshoot agent used after agent stuck in loop: NO
  - Test files created: 
  - Known regressions: Race condition in duplicate comment logic.

**Credentials to test flow:**
Check .
Admin Email: 

**What agent forgot to execute**
The agent correctly implemented the business logic check () but forgot to account for async race conditions when multiple bot tasks process concurrently during a burst. A database-level unique index (or a strict application-level lock) is required to pass the test suite.

**Debugging checklist for current issue**
    Issue: Bot Multiple Comments Race Condition (P0)

    Backend files to check: 
    -  (Line ~5268+ inside  to add a unique index to MongoDB).
    -  (Review  and  to handle potential  exceptions gracefully).

    Database to check:
    - MongoDB collections where comments/replies are stored need an index ensuring  and  combination is strictly unique.</analysis>
